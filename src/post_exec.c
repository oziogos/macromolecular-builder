
#include"builder.h"

void post_exec(int myseed)
{
    char command[cmax_length];
    /*
    sprintf(command,"cp mol_1.lammps mol_1_%d.lammps",myseed);printf("%s\n",command);system(command);
    sprintf(command,"cp preview.dat preview_%d.dat",myseed);printf("%s\n",command);system(command);
    sprintf(command,"cp preview_periodic.dat preview_periodic_%d.dat",myseed);printf("%s\n",command);system(command);
    sprintf(command,"cp preview.mol2 preview_%d.mol2",myseed);printf("%s\n",command);system(command);
    sprintf(command,"cp Cn_history.dat Cn_history_%d.dat",myseed);printf("%s\n",command);system(command);
    sprintf(command,"cp Cn_vs_build.dat Cn_vs_build_%d.dat",myseed);printf("%s\n",command);system(command);
    sprintf(command,"cp tacticity_history.dat tacticity_history_%d.dat",myseed);printf("%s\n",command);system(command);
    */
}
